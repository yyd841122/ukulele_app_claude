<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- MVP: no INTERNET permission. Offline only. -->
    <!-- T005: required for tuner + recording. Not granted until user triggers tuner/recording. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
</manifest>